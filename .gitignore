.pio

# Generated LittleFS assets — rebuilt by `pio run` (minify + prepare_littlefs).
# (The build scripts in src/web/build/ stay tracked; that dir has its own
#  .gitignore that excludes only the generated minified output.)
data/
.littlefs_hash

# VS Code (generated)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Pre-rebrand backups (kept locally, not pushed)
*.pristonchess.bak

# Local diagnostics / scratch
a3144_test/
Bildschirmfoto*.png

# OTA-Release-Artefakte (werden an GitHub-Releases gehängt, nicht getrackt)
firmware.bin
web_assets.tar
